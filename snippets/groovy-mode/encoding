# -*- mode: snippet; require-final-newline: nil -*-
# name: Idiom: Set up a encoding of compiled files
# key: encoding
# --
tasks.withType(AbstractCompile).each { it.options.encoding = "${1:encoding}" }
$0